# Instructions
1. Install dependencies by running `npm install` from the root.
2. Generate the game database by running `node backend/populateDatabase.js`.
3. Run the web server with `node backend/server.js`.
4. In a browser, open localhost:3000.
